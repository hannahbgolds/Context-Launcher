# Default icons

Context Launcher does not ship raster default-icon assets. Its built-in icons
are SF Symbol names stored with each context and rendered into each generated
launcher bundle at save or install time.

This directory documents that deliberate absence. Custom images selected in the
app are copied into the user's local Context Launcher support directory and
are not repository assets.
